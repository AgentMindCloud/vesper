# Vesper environment variables
# Copy this file to .env and fill in real values.
# Never commit .env

# Required for live mode (xAI / Grok)
XAI_API_KEY=your_xai_api_key_here

# Required for live X context + mentions / replies
X_BEARER_TOKEN=your_x_bearer_token_here
# Optional OAuth if you need user-context posting
X_OAUTH_TOKEN=

# Required for real-time voice (STT/TTS)
GROK_VOICE_API_KEY=your_grok_voice_key_here

# Kill switch (set to 1 to halt all Vesper activity)
# VESPER_DISABLED=0

# Optional overrides
# VESPER_MEMORY_CROSS_SESSION=false
# VESPER_PROACTIVE_ENABLED=true
